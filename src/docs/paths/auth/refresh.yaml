post:
  tags: 
    - Authentification
  description: Refresh user's session
  summary: Refresh
  requestBody:
    content:
      'application/json':
        schema:
          $ref: ../../components/schemas/Refresh/index.yaml
  responses:
    '200':
      description: User has refreshed token
