post:
  tags: 
    - Authentification
  description: Login an existing user
  summary: Login
  requestBody:
    content:
      'application/json':
        schema:
          $ref: ../../components/schemas/Login/login.yaml
  responses:
    '200':
      description: User has logged in
