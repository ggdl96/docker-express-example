pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile'
        }
    }
    stages { 
        stage('Example') {
            steps {
                echo 'DONE!'
            }
        }
    }
}
